{
  "name": "intergalactic-traffic",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Intergalactic Traffic Simulator - an incremental game about being route authority for the light cone.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "esbuild tools/check.ts --bundle --platform=node --format=esm --outfile=.check.mjs --log-level=warning && node .check.mjs"
  },
  "dependencies": {
    "break_infinity.js": "^2.0.0",
    "lz-string": "^1.5.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
